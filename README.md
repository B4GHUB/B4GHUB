آموزش ساخت کانفینگ روی سایت پایین هست کلیک کنید بعد روبات نیستید تایید کنید بعد کد امنیتی بزنید ورود بشوید بعد بالای سایت نوشته کپی کردن کانفیگ ها بزنید بعد استفاده کنید

This contains everything you need to run your app locally.

View your app in AI Studio: https://b4ghub.ai.studio

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
